# VULKRAN OS — Caddy reverse proxy (HTTPS via Let's Encrypt)

api.vulkran.com {
    handle /files/* {
        uri strip_prefix /files
        root * /data
        file_server
    }

    reverse_proxy api:8000
}

n8n.vulkran.com {
    reverse_proxy n8n:5678
}
